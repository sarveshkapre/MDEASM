[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# Keep rules minimal to avoid forcing a large style refactor of the upstream script code.
select = ["E9", "F63", "F7", "F82"]

